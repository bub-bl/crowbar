@inherits Crowbar.UI.PropertyEditorBase
@namespace Crowbar.Editor.Ui.Components
@using Crowbar.UI
@attribute [EditorProperty(typeof(bool))]

<div class="prop-row" style="padding-left: @(Indent * 12)px">
    <div class="field-label">@Name</div>
    <input type="checkbox" checked="@IsChecked" @onchange="OnChanged" />
</div>

@code {
    private bool IsChecked => bool.TryParse(Value, out var parsed) && parsed;

    private void OnChanged(bool value)
    {
        Value = value ? "true" : "false";
        EditorInspectorState.RequestEdit(Key, Value);
    }

    protected override int BuildHash() => HashCode.Combine(Name, Value, Key, Indent);
}
